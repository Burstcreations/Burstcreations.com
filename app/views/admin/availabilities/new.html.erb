<%= form_for @availability, :url => { :action => "create" } do |f| %>
  <%= date_select(:availability, :date, :order => [:month, :day]) %>
  <%= f.label :enabled,  :value => "Enabled" %>
  <%= f.check_box :enabled, :checked => true %>
  <%= f.submit "Create", :class => "btn btn-primary"%>
<% end %>